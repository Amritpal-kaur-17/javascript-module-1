const namesArray = ["Daniel", "James", "Irina", "Mozafar", "Ashleigh"];
function findname(peresonname){
    if(name.find(name => names === personname)){
        return foung menubar;
    }
    else{
        return not found me: (";
    }
}
console.log(findname("emmy"));
   

