const NumberOfStudents = 10;
const NumberOfmentor = 5;
var sum = NumberOfStudents + NumberOfmentor;
const percentage_of_students = Math.round((NumberOfStudents/sum)*100);

const percentage_of_mentor = Math.round((NumberOfmentor/sum)*100);
console.log(percentage_of_students);
console.log(percentage_of_mentor);